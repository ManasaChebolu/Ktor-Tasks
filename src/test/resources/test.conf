ktor {
    development=true
    deployment {
        port = 8081
        port = ${?PORT}
        watch=[classes,resources]
    }
    application {
        modules = [com.panDetails.ApplicationKt.module]
    }
}

storage{
   url = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;INIT=create domain if not exists jsonb as other;"
    driver = "org.h2.Driver"
    user = "postgres"
    pass = "root"
}


